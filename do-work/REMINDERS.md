# Reminders

## Chrome: Stop treating URLs as searches
- **Cmd+L** before typing any `chrome://` URLs
- Look into Chrome settings (`chrome://settings/search`) to reduce aggressive search behavior
- Consider switching default new tab behavior or installing an extension that fixes omnibox behavior
