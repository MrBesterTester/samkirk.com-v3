---
id: REQ-063
title: "Rewrite hire-me page"
status: completed
created_at: 2026-02-13T00:00:00Z
user_request: UR-008
claimed_at: 2026-02-13T06:00:00Z
route: B
completed_at: 2026-02-13T06:10:00Z
related: [REQ-058, REQ-059, REQ-060, REQ-061, REQ-062]
batch: "hire-me-unified-phase-3"
source_step: "3.1"
source_doc: "docs/hire-me-unified-TODO.md"
blueprint_ref: "docs/hire-me-unified-BLUEPRINT.md"
model_hint: "Opus 4.5"
---

# Rewrite hire-me page (Step 3.1)

## What
Rewrite `web/src/app/hire-me/page.tsx` from a hub/link page to the unified chat-first page. Compose all Phase 1 components and wire them through the useHireMe hook. JobContextBar sits outside ToolGate; ChatStream + actions bar + ChatInput sit inside ToolGate.

## Checklist
- [x] Rewrite `web/src/app/hire-me/page.tsx` as unified chat page
- [x] Page header + description at top
- [x] JobContextBar outside ToolGate
- [x] ToolGate wrapping ChatStream + actions bar + ChatInput
- [x] Wire all components through `useHireMe` hook
- [x] Actions bar with downloads + "New Conversation" button

## Blueprint Guidance
**`web/src/app/hire-me/page.tsx`** — Rewrite from hub to unified page
- Imports all components above
- Wraps chat area in `ToolGate` (single captcha for all tools)
- Page header + description
- Composes: JobContextBar + ToolGate(ChatStream + ChatInput)

Layout from specification:
```
┌─────────────────────────────────────────┐
│ Hire Me — page header + description     │
├─────────────────────────────────────────┤
│ Job Context Bar (outside ToolGate)      │
│ ToolGate {                              │
│   ChatStream (scrollable)               │
│   Actions bar (downloads + new convo)   │
│   Preset chips (when job loaded)        │
│   ChatInput (textarea + send)           │
│ }                                       │
└─────────────────────────────────────────┘
```

## Context
- **Document set**: hire-me-unified
- **Phase**: 3 — Assemble the Page
- **Specification**: See docs/hire-me-unified-SPECIFICATION.md for full requirements
- **Model recommendation**: Opus 4.5 (advisory)

## Dependencies
Depends on Phase 1 (all components) and Phase 2 (useHireMe hook).

---
*Source: docs/hire-me-unified-TODO.md, Step 3.1*

---

## Triage

**Route: B** - Medium

**Reasoning:** Clear composition task — all components already built in Phase 1/2. Need to find existing component APIs and the current page structure to rewrite.

**Planning:** Not required

## Plan

**Planning not required** - Route B: Exploration-guided implementation

Rationale: All child components and the useHireMe hook already exist. This is a page composition task with a clear layout spec. Just need to discover component APIs and current page structure.

*Skipped by work action*

## Exploration

**Current page** (`web/src/app/hire-me/page.tsx`): Simple hub with 3 Link cards to `/hire-me/fit`, `/hire-me/resume`, `/hire-me/interview`. Server component. Will be fully rewritten.

**Components available** (all from `@/components/hire-me`):
- `JobContextBar` — props: `{ onJobLoaded, onJobCleared, isLoading?, jobTitle?, jobCompany? }`
- `ChatStream` — props: `{ messages[], isLoading?, onRetry?, onAnswer?, answeredQuestions? }`
- `ChatInput` — props: `{ onSend, onPreset, isLoading?, jobLoaded?, flowActive?, disabled? }`
- `FitReportCard`, `ResumePreviewCard`, `FitQuestionCard` — rendered inside ChatStream via MessageRenderer

**Hook** (`@/hooks/useHireMe`):
- Returns: `{ state, loadJob, clearJob, triggerFit, triggerResume, answerFitQuestion, sendMessage, newConversation, download, jobLoaded, flowActive, jobTitle, jobCompany }`
- `state.messages: ChatMessage[]`, `state.downloads: DownloadEntry[]`, `state.isLoading`, `state.answeredQuestions`

**ToolGate** (`@/components/ToolGate`):
- Props: `{ children, toolName? }` — wraps children behind session init + captcha gate

**Styling pattern**: Tailwind, `max-w-4xl` container, `px-4 py-16 sm:px-6 lg:px-8`, `text-text-primary`/`text-text-secondary` design tokens.

*Generated by Explore agent + orchestrator verification*

## Implementation Summary

- Rewrote `web/src/app/hire-me/page.tsx` from server component hub page to `"use client"` chat-first page
- Composes JobContextBar (outside ToolGate), ChatStream + actions bar + ChatInput (inside ToolGate)
- Actions bar shows download buttons per `state.downloads` entry + "New Conversation" button
- All state wired through `useHireMe` hook; preset chips mapped to `triggerFit`/`triggerResume`
- ChatStream in fixed-height scrollable container (`h-[28rem] sm:h-[32rem]`)

*Completed by work action (Route B)*

## Testing

**Tests run:** `npx vitest run`
**Result:** ✓ All tests passing (1232 tests, 38 files)

**New tests added:** None — page composition, no new logic to unit test

**Existing tests verified:** Full suite passes, no regressions

*Verified by work action*
